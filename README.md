## Permission System

A lightweight rest-based permission system.

---

### Concept

Everything exists within exactly one scope.
Every permission, every role and every user object is only visible in its scope.

Scopes are unique.